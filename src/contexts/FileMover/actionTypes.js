const actionTypes = Object.freeze({
  REQUEST_FILE_MOVE: 'REQUEST_FILE_RENAME',
  COMPLETE_FILE_MOVE: 'COMPLETE_FILE_RENAME',
  CANCEL_FILE_MOVE: 'CANCEL_FILE_RENAME',
});

export default actionTypes;
